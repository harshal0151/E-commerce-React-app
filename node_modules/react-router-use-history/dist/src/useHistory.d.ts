/// <reference types="react" />
import { UNSAFE_DataRouterContext } from 'react-router-dom';
import { type History as RemixHistory } from '@remix-run/router';
import { type BrowserRouterHistoryState } from './BrowserRouter';
/**
 * data browser router history hooks
 */
export declare const useBrowserRouterHistory: () => History;
declare type ExtractContextValueType<C = any> = C extends React.Context<infer U> ? U : never;
declare type DataRouterContextObject = ExtractContextValueType<typeof UNSAFE_DataRouterContext>;
declare type Listen = NonNullable<DataRouterContextObject>['router']['subscribe'];
/**
 * data browser router state
 * @example history.listen((state: DataRouterHistoryState) => {})
 */
export declare type DataRouterHistoryState = Parameters<Parameters<Listen>[0]>[0];
/**
 * router state when history change
 * @example history.listen((state: RouterState) => {})
 */
export declare type RouterState = Partial<DataRouterHistoryState> & Partial<BrowserRouterHistoryState> & Pick<BrowserRouterHistoryState, 'location'>;
/**
 * history listener function type
 */
export declare type Listner = (listener: (state: RouterState) => void) => () => void;
declare type HistoryListen = {
    listen: Listner;
};
/**
 * compatible with `<BrowserRouter>` and `createBrowserRouter` history
 */
export declare type History = Omit<RemixHistory, 'listen'> & HistoryListen;
export declare const useHistory: () => History;
export {};
